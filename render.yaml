services:
  - type: web
    name: codekids
    env: docker
    region: oregon
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: "gpt-4o-mini"
